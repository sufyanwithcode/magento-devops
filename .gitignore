# Secrets — never commit real env
.env

# Local compose override
docker-compose.override.yml

# Magento application code (installed via Composer, large)
/src/*
!/src/.gitkeep

# Container data
docker/mysql/data/

# Backup dumps
backups/dumps/

# Logs & OS cruft
*.log
.DS_Store
*.swp
