[local]
localhost ansible_connection=local

[cloud]
# Fill in after `terraform apply` gives you the public IP:
# cloud-vm ansible_host=1.2.3.4 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[magento:children]
local
cloud
